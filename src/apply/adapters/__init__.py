"""ATS adapter package.

Each adapter implements the `ATSAdapter` Protocol from `src.apply.base`.
S8 owns `greenhouse.py` and the shared `_labels.py` helper; future shards
(S22 Lever, S23 Ashby) reuse the same label-scan helper.
"""
