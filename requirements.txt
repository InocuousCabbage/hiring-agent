# Core
pyyaml>=6.0
python-dotenv>=1.0
anthropic>=0.39.0  # optional: only needed if using ANTHROPIC_API_KEY instead of Claude CLI

# Gmail API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Email parsing
beautifulsoup4>=4.12
lxml>=5.0

# Web scraping
httpx>=0.27
playwright>=1.40
readability-lxml>=0.8

# Document generation
python-docx>=1.1
# LibreOffice CLI is used for DOCX→PDF (install via apt, not pip)

# PDF utilities (for QA page-count checks)
pypdf>=4.0

# Logging + utilities
structlog>=24.0
tenacity>=8.2
