# Secrets
.env
config/credentials/
token.json

# Personal resume templates (user-created, not shipped with repo)
templates/resumes/*.docx
templates/cover_letter.docx

# Personal data files (not shipped with repo)
templates/candidate_profile.yaml

# Local state (dedup DB, run history)
state/

# Python
__pycache__/
*.pyc
.venv/

# Output
output/
test_data/output/
logs/

# OS
.DS_Store
.personal-backup/
# Sample email files (contain personal email headers)
test_data/*.eml

# OBF/m2c1 planning artifacts (local working docs, not part of repo)
.agent/

# Local code-review + planning scratch (per-run reviewer outputs, not shipped)
reviews/
